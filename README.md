# 🤖 Minecraft Bedrock Chatbot (Gemini Powered)

هذا المشروع البسيط هو ربط بين ماين كرافت (Bedrock Edition) مع Google Gemini API للتحدث مع اللاعبين، مع حل كامل لمشكلة ظهور اللغة العربية في اللعبة.

## 🌟 الميزات

* **ذكاء اصطناعي:** يستخدم نموذج `gemini-2.5-flash` للردود السريعة.
* **دعم اللغة العربية:** يتضمن نظام معالجة مخصص (`arabic-fix.js`) يقوم بتشكيل وعكس النص العربي ليظهر بشكل صحيح في بيئة اللعبة (LTR)، مع الحفاظ على الأرقام والكلمات الإنجليزية في أماكنها.
* **سجل محادثة:** يتذكر البوت سياق الحديث لآخر 6 رسائل لكل لاعب.
* **مظهر مخصص:** يمكن تحميل Skin مخصص للبوت عبر ملف `ai_skin_data.json`.
* **مفاتيح متعددة:** يدعم قائمة بمفاتيح API للتبديل بينها لتجنب التقييد (Rate Limits).

## 🛠️ المتطلبات

* [Node.js](https://nodejs.org/) (الإصدار 14 أو أحدث).
* سيرفر ماين كرافت Bedrock يعمل.

## 🚀 التثبيت والتشغيل

1.  **تثبيت الحزم المطلوبة:**
    افتح التيرمينال في مجلد المشروع واكتب:
    ```bash
    npm install bedrock-protocol
    ```

2.  **الإعداد:**
    افتح ملف `index.js` وقم بتعديل البيانات التالية في الأعلى:
    ```javascript
    const ServerHost = "your-server.net"; // عنوان السيرفر
    const ServerPort = 19132;             // المنفذ
    
    const GEMINI_API_KEYS = [ 
        'ضع_مفتاح_API_هنا', 
    ];
    ```

3.  **التشغيل:**
    ```bash
    node index.js
    ```

## 📂 هيكلية الملفات

* `index.js`: الكود الأساسي للاتصال بالسيرفر وإدارة المحادثات.
* `arabic-fix.js`: مكتبة معالجة النصوص (Shaping & Reordering) لتوافق ماين كرافت.
* `ai_skin_data.json`: ملف يحتوي على بيانات سكن البوت (اختياري).

## ⚠️ ملاحظة هامة
لا تقم برفع مفاتيح API الخاصة بك على GitHub بشكل علني. تأكد من إزالتها أو استخدام متغيرات البيئة (.env) قبل نشر الكود.





